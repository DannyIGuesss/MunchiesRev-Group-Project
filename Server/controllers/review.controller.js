const Reviews = require('../models/review.model');

module.exports = {
    postReview: async (req, res) => {
        try {
            const review = req.body
            // TODO: complete this once jwt.config file is ready
            // ! need the req.user._id from jwt.config file
            // const user_id = 
        }
    }
}